{
  "name": "realmora",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --d1=DB",
    "deploy": "wrangler pages deploy .",
    "db:migrate:local": "wrangler d1 migrations apply realmora --local",
    "db:migrate:remote": "wrangler d1 migrations apply realmora --remote",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "typescript": "^5",
    "wrangler": "^4"
  }
}
